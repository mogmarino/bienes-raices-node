extends ../layout/admin

block styles
  link(rel="stylesheet" href="https://unpkg.com/leaflet@1.8.0/dist/leaflet.css")

block contenido 

   .py-10
      h1.text-4xl.my-10.font-extrabold.text-center Bienes
        span.font-normal Raices
      h2.text-center.text-2xl.font-extrabold.text-orange-700= pagina

     
      div(class="bg-white shadow py-8 px-4 rounded mx-auto my-10 max-w-4xl md:px-10")
        form.space-y-8(action="/propiedades/crear" method="POST")
            input(type="hidden", name="_csrf" value= csrfToken)
            
            input(class="w-full py-3 bg-indigo-500 hover:bg-indigo-700 rounded text-white font-bold uppercase cursor-pointer transition-all" type="submit" value="Publicar Propiedad")

                    
block scripts
  
