header.bg-indigo-600.p-4 
    .container.mx-auto.flex.justify-between.items-center
        a(href="/")
            h1.text-2xl.text-white.font-extrabold.text-center Bienes 
                span.font-normal Raices

        nav(class="my-5 text-sm md:flex md:items-center md:gap-4 font-bold text-white hidden")
            a(href="/mis-propiedades" class="") Mis Propiedades
            a(href="#" class="") Mi Perfil
            form(action="/auth/cerrar-sesion" method="POST")
                input(type="hidden" name="_csrf" value=csrfToken)
                input(type="submit" class="bg-indigo-800 rounded-lg px-10 py-2 hover:bg-gray-200 hover:text-indigo-900 transition-all" value="Cerrar Sesión")
